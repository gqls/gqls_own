server {
    listen 80;
    server_name localhost;

    root /var/www/project/frontend/dist/;
    index index.html;

    sendfile off;

    location / {
        try_files $uri $uri/ =404;
    }
}